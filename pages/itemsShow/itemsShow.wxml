<!-- TODO: change the below with show one item only code -->
<view>
<image class="item-image-large" src="{{item.image_url}}" lazy-load="true"></image>
</view>

<view>
    <view class="flex-container">
        <view class="make-like-button3">{{item.category}}</view>
        <view class="addpadding">
            <text>{{item.name}}</text>
        </view>
    </view>
</view>
        <view class="make-like-button2">可兑换</view>

    <view class="flex-container">
        <view class="make-like-button1">要求:</view>
        <view class="addpadding2">
            <text>{{item.request}}</text>
        </view>

    </view>



    <view class="addmargin">
        <text>{{item.description}}</text>
        <text>{{item.id}}</text>
    </view>
<button class="icon-box-img" data-id="{{item.id}}" bindtap="goToBidsNew"><icon type="success" size="70"></icon></button>
